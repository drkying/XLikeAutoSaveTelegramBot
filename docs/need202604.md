1. 使用官方 Telegram Bot API 时 R2 仍在大于50MB时使用；使用自建 Telegram Bot API 时 R2 只有在大于2000MB的文件才使用，小于的请通过数据库保存telegram生成的链接，建立链接对应关系
2. 使用R2保存时，现在bot发送给用户的是一条x推文链接和文件R2链接，我希望发送推文的markdown内容，仅仅是将原本推文中的视频替换为R2文件链接而已
3. 我希望每个x账号可以独立配置他的相关配置，可以独立配置x api的client和secret 而不是现在这样一个telegram用户只能配置一个api
4. 现在在telegram bot中 输入框输入‘/’时，没有命令自动补齐，并且没有按钮提供选择
5. 避免报错：
Error: Too many subrequests by single Worker invocation. To configure this limit, refer to https://developers.cloudflare.com/workers/wrangler/configuration/#limits
Error: Too many subrequests by single Worker invocation. To configure this limit, refer to https://developers.cloudflare.com/workers/wrangler/configuration/#limits
"Error: Bad Request: failed to send message #1 with the error message "WEBPAGE_MEDIA_EMPTY" at callTelegramApi (index.js:2678:13) at async sendMediaGroup (index.js:2889:20) at async persistTweetMedia (index.js:9427:19) at async handleTweet (index.js:9544:3) at async pollAccount (index.js:9590:7) at async pollAllAccounts (index.js:9642:5) at async index.js:9724:11",

6. 请考虑取消喜欢的情况，是否会影响正常功能
7. 能否提供可视化的轮询设置
8. 提供手动触发保存的按钮
9. kv、d1、r2的相关信息改为从环境变量读取，并且清除git历史记录中存储过的
